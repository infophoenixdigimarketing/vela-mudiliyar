RewriteEngine On
# Pass Authorization header through to PHP
SetEnvIf Authorization "(.*)" HTTP_AUTHORIZATION=$1
# Route everything to index.php?route=...
RewriteCond %{REQUEST_FILENAME} !-f
RewriteRule ^(.*)$ index.php?route=$1 [QSA,L]
